
  
https://lab.github.com/githubtraining/introduction-to-github

vpn N4 seems to work

https://pages.github.com/
https://www.finex.co/how-to-display-html-in-github/

--------------------------------------------------------
--------------------------------------------------------

How to prepare a github
https://www.tutsmake.com/upload-project-files-on-github-using-command-line/
cd/your project directory
 1) git init
 2) git add . or git add ['filename']
 3) git commit -m "My first File"
  git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

 4) git remote add origin https://github.com/mariamessineo/KMclassI-DR2.git
 5) git pull origin master
 6) git push origin master

git push
https://github.com/git-guides/git-push
token is requested for git push
https://github.blog/2020-12-15-token-authentication-requirements-for-git-operations/
https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/token-expiration-and-revocation

non fast
https://docs.github.com/en/get-started/using-git/dealing-with-non-fast-forward-errors
git fetch origin
git push origin master --force



CORRECTED
from inside docs
git init
git add 
git commit -m "My first File"
git remote add origin https://github.com/mariamessineo/KMclassI-DR2.git
git fetch origin
git push origin master --force
 
 
 CORRECT
 from main
 1) git init
 2) git add . or git add ['filename']
 3) git commit -m "My first File"
 4) git remote add origin https://github.com/mariamessineo/KMclassI-DR2.git
 5) git fetch origin  
 6) git push origin main --force
------------------------------------

git clone https://github.com/mariamessineo/KMclassI-DR2.git
cd KMclassI-DR2 &&   cp /Users/mmessine/HUNTING/newRSG/known/chartes/IMAGES/wise12_50*.jpg  docs/. 
git add . 
git commit -m "My first File"
git remote add origin https://github.com/mariamessineo/KMclassI-DR2.git
git push
 
git  clone --single-branch https://github.com/mariamessineo/KMclassI-DR2.git
git checkout -b master 
cd KMclassI-DR2 &&   cp /Users/mmessine/HUNTING/newRSG/known/chartes/IMAGES/wise12_*.jpg  docs/. 
cd KMclassI-DR2 &&   cp /Users/mmessine/HUNTING/newRSG/known/chartes/IMAGES/kmap*.jpg  docs/. 
cd KMclassI-DR2 &&   cp /Users/mmessine/HUNTING/newRSG/known/chartes/IMAGES/*.wise.jpg  docs/. 
cd KMclassI-DR2 &&   cp /Users/mmessine/HUNTING/newRSG/known/chartes/IMAGES/*.2mass.jpg  docs/. 
git checkout -b master 
git add . 
git commit -m "My first File"
git remote add origin https://github.com/mariamessineo/KMclassI-DR2.git
git pull origin master
git push  origin master

-------------------------------------------------------------

https://stackoverflow.com/questions/45565464/git-push-subfolder-to-different-repository

using subfolder
https://ningrumm.medium.com/push-files-in-specific-folder-on-github-f25dd2ddec21

git add ./docs

https://gist.github.com/tswaters/542ba147a07904b1f3f5
# add jsdoc remote, create new tracking branch, 
git remote add  origin https://github.com/mariamessineo/KMclassI-DR2.git
git checkout  origin/master

# split off subdir of tracking branch into separate branch
git subtree split -q --squash --prefix=templates/default  --rejoin -b merging/origin

# add separate branch as subdirectory on master.
git checkout master
git subtree add --prefix=jsdoc-template --squash merging/jsdoc
https://docs.github.com/en/get-started/using-git/about-git#basic-git-commands
git read-tree --prefix=pollo/ -u origin/main


------------------------------------------------------
COMMAND to clone only a subdirectory of a repository
------------------------------------------------------
mkdir LALA
cd LALA
git init
git remote add -f origin https://github.com/lamortadella/BPRPlibraries
git config core.sparseCheckout true
echo "DAWD_spectra" >> .git/info/sparse-checkout
git checkout -b main
git pull origin main


